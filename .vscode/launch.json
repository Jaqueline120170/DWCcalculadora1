{
    "version": "0.2.0",
    "configurations": [
      {
        "type": "chrome",
        "request": "launch",
        "name": "Launch Chrome against localhost",
        "url": "http://localhost:5500",  // Asegúrate de que el puerto sea el correcto.
        "webRoot": "${workspaceFolder}"
      }
    ]
  }
  